{
  "name": "timeloop",
  "version": "1.0.0",
  "private": true,
  "description": "AI-powered time tracking app with Chrome extension website tracking.",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node server/index.js",
    "build:extension": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-extension.ps1",
    "test": "node scripts/smoke-test.js"
  },
  "dependencies": {
    "mongodb": "^6.16.0"
  },
  "engines": {
    "node": ">=18"
  }
}
